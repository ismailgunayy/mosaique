// Actual dimensions of the canvas
export const canvasWidth = window.innerWidth;
export const canvasHeight = window.innerHeight - 96;

// Maximum allowed dimensions for the drawing area
export const maxCanvasWidth = 750;
export const maxCanvasHeight = 750;
